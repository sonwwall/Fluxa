## 本地启动约定补充

- 目标：明确前后端开发服务启动时统一加载仓库根目录 `.env`，并固定本地开发端口。
- 主要文件：
  - `AGENTS.md`：新增本地启动约定，说明前端使用 `3001` 端口，后端使用 `8080` 端口。

## 前端完成标准补充

- 目标：要求前端功能完成前访问对应页面，确认页面可正常打开且 Next.js 无报错。
- 主要文件：
  - `docs/前端开发规范.md`：在测试与完成标准中补充页面访问和 Next.js 报错检查要求。
