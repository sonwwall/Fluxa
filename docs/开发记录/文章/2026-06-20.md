## Markdown 折叠内容渲染

- 目标：修复文章 Markdown 中 `<details><summary>` 折叠内容被展开显示的问题，同时保持用户 HTML 默认安全过滤。
- 主要文件：
  - `fluxa-web/src/features/articles/components/markdown-renderer-core.ts:10`：新增 Markdown 分段类型和 `<details>` 块解析。
  - `fluxa-web/src/features/articles/components/markdown-renderer.tsx:17`：发布文章渲染按分段输出折叠块。
  - `fluxa-web/src/features/articles/components/markdown-preview.tsx:18`：编辑预览渲染按分段输出折叠块。

## Markdown 标题层级样式

- 目标：重新设计文章 Markdown 中 H1-H6 的标题样式，增强三级以下标题的视觉区分度。
- 主要文件：
  - `fluxa-web/src/features/articles/components/markdown-components.tsx:57`：新增 H1-H6 统一标题渲染组件和各层级装饰样式。
  - `fluxa-web/src/features/articles/components/markdown-renderer-core.ts:88`：按 1 到 6 级分别定义标题字号、间距和文字权重。

## 文章阅读字号调整

- 目标：在文章阅读页右下角提供字号调整窗口，支持不同设备下切换正文阅读大小并记住选择。
- 主要文件：
  - `fluxa-web/src/features/articles/components/article-reading-font-controls.tsx:19`：新增阅读字号浮动控件和本地持久化。
  - `fluxa-web/src/features/articles/components/article-detail-page.tsx:27`：将文章正文包裹到字号控制容器中。
  - `fluxa-web/src/features/articles/components/markdown-renderer-core.ts:88`：标题字号接入阅读字号变量。

## 文章阅读字号档位调整

- 目标：将阅读字号档位调整为 80%、90%、100%、110%，并将 110% 命名为宽松。
- 主要文件：
  - `fluxa-web/src/features/articles/components/article-reading-font-controls.tsx:8`：更新字号选项标签和缩放比例。

## Markdown 标题紧凑化

- 目标：适当降低文章 Markdown 标题字号，并缩小标题与正文之间的间距。
- 主要文件：
  - `fluxa-web/src/features/articles/components/markdown-renderer-core.ts:88`：下调 H1-H6 字号和标题外边距。
  - `fluxa-web/src/features/articles/components/markdown-components.tsx:157`：压缩标题装饰图标、间距和分隔线距离。

## 文章阅读字号控件收纳

- 目标：将阅读字号浮动窗口默认收纳到右侧，仅保留图标按钮，点击后展开设置面板。
- 主要文件：
  - `fluxa-web/src/features/articles/components/article-reading-font-controls.tsx:20`：新增面板展开状态和右侧收纳触发按钮。

## 文章目录标题文案

- 目标：将文章详情页右侧目录标题从 `On this page` 调整为 `Contents`。
- 主要文件：
  - `fluxa-web/src/features/articles/components/article-detail-page.tsx:148`：更新目录标题文案。

## Markdown 链接高亮

- 目标：增强文章 Markdown 链接的视觉高亮，避免链接文字与普通正文混在一起。
- 主要文件：
  - `fluxa-web/src/features/articles/components/markdown-components.tsx:20`：为链接增加强制高亮颜色、下划线、浅色背景和 hover 状态。

## 作者文章发布流转修复

- 目标：修复已发布文章撤回后进入归档状态且无法再次发布的问题，并确保点击发布前会保存当前编辑内容。
- 主要文件：
  - `fluxa-server/internal/article/service/withdraw_author_article.go:12`：撤回文章后回到草稿状态，并清空发布时间和定时时间。
  - `fluxa-server/internal/article/service/publish_author_article.go:15`：允许旧归档文章再次发布，兼容已受影响的数据。
  - `fluxa-server/internal/article/service/save_author_article.go:60`：允许旧归档文章保存编辑内容。
  - `fluxa-server/internal/article/service/article_status_transition_test.go:1`：新增发布、撤回状态流转回归测试。
  - `fluxa-web/src/features/author/api/author.ts:72`：发布和撤回接口返回后端状态结果。
  - `fluxa-web/src/features/author/components/article-editor-page.tsx:49`：发布前先保存当前编辑内容，并按后端返回状态更新界面。
