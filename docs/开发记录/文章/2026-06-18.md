# 2026-06-18

## 文章目录折叠

- 目标：文章详情页右侧目录默认只显示最高层级标题，点击小三角后展开更小层级标题。
- 文件：`fluxa-web/src/features/articles/components/article-detail-page.tsx:135`。
- 内容：将 Markdown 标题构造成目录树，使用原生 `details/summary` 渲染可折叠层级，并保留标题锚点跳转。

## 文章目录层级样式调整

- 目标：修正可展开目录项和普通目录项的同级对齐，并把展开提示放进左侧圆点。
- 文件：`fluxa-web/src/features/articles/components/article-detail-page.tsx:192`。
- 内容：隐藏原生 `summary` marker，移除叶子节点多余占位，并在有子标题的圆点内显示可旋转三角。

## 文章目录当前位置高亮

- 目标：右侧目录的左侧圆点跟随当前阅读位置点亮，不再固定高亮第一项。
- 文件：`fluxa-web/src/features/articles/components/article-detail-page.tsx:136`、`fluxa-web/src/features/articles/components/article-toc-client.tsx:19`。
- 内容：将目录交互拆到客户端组件，监听滚动和窗口尺寸变化，根据当前标题锚点更新高亮状态，并保留折叠交互。

## Markdown 渲染能力补齐

- 目标：补齐文章详情页和作者预览中常见 Markdown 语法的渲染，避免原始符号直接显示。
- 文件：`fluxa-web/src/features/articles/components/markdown-renderer-core.tsx:1`、`fluxa-web/src/features/articles/components/markdown-renderer.tsx:36`、`fluxa-web/src/features/articles/components/markdown-preview.tsx:33`。
- 内容：支持删除线、斜体、行内代码、链接、图片、无序/有序/任务列表、分割线、表格和公式块的基础展示。

## Markdown 渲染框架替换

- 目标：用成熟 Markdown 渲染链路替换自研解析器，统一前台文章展示和后台编辑预览效果。
- 文件：`fluxa-web/src/features/articles/components/markdown-renderer.tsx:1`、`fluxa-web/src/features/articles/components/markdown-preview.tsx:1`、`fluxa-web/src/features/articles/components/markdown-components.tsx:1`、`fluxa-web/src/features/articles/components/markdown-renderer-core.ts:1`、`fluxa-web/src/app/globals.css:1`、`fluxa-web/package.json`。
- 内容：引入 `react-markdown`、`remark-gfm`、`remark-math`、`rehype-slug`、`rehype-sanitize`、`rehype-katex`、`katex`、`@tailwindcss/typography`，保留 Shiki 作为前台代码块高亮，并使用 `github-slugger` 统一目录和正文标题锚点。

## Markdown 行内代码 Hydration 修复

- 目标：修复文章详情页中行内代码被误渲染成块级 `<div>` 后导致的 `<p>` 嵌套错误和 hydration 报错。
- 文件：`fluxa-web/src/features/articles/components/markdown-components.tsx:40`。
- 内容：仅在 Markdown 代码节点带有 `language-*` 类名时才进入 Shiki 块级渲染，普通行内代码保持 `<code>` 输出。

## 文章目录折叠高亮修正

- 目标：修复当前阅读标题处于折叠目录内部时，右侧目录高亮不可见的问题。
- 文件：`fluxa-web/src/features/articles/components/article-toc-client.tsx:22`。
- 内容：保留真实滚动激活标题，同时根据当前展开状态计算可见高亮标题；折叠时高亮最近可见父级，展开后恢复到真实子标题。

## Markdown 代码块样式调整

- 目标：去掉代码块深色外壳包浅色代码区的双层背景，改为统一协调的深蓝灰背景和细亮色边框。
- 文件：`fluxa-web/src/features/articles/components/markdown-renderer.tsx:27`、`fluxa-web/src/features/articles/components/markdown-components.tsx:117`。
- 内容：前台 Shiki 代码块只保留一层 `pre` 背景并统一为 `#151c2e`，后台预览代码块同步使用相同背景和边框。
