## 系统文案中英切换

- 目标：为系统级界面文案提供中文/英文切换，不翻译文章正文和业务数据内容。
- 主要文件：
  - `fluxa-web/src/features/i18n/i18n.tsx:10`：新增全局语言状态、翻译表、Hook 和客户端文本组件。
  - `fluxa-web/src/app/providers.tsx:3`：接入全局语言 Provider。
  - `fluxa-web/src/features/articles/components/top-navigation.tsx:21`：在顶部导航右侧新增中英切换按钮，并翻译导航和搜索文案。
  - `fluxa-web/src/features/articles/components/article-detail-page.tsx:1`：翻译文章详情页系统文案。
  - `fluxa-web/src/features/author/components/admin-shell.tsx:1`：翻译作者后台框架、侧栏和面包屑文案。

## 语言切换控件样式调整

- 目标：将顶部导航的中英切换入口从圆形标志改为 CN/EN 双段滑动按钮，降低视觉突兀感。
- 主要文件：
  - `fluxa-web/src/features/articles/components/top-navigation.tsx:70`：调整语言切换按钮结构、尺寸、滑块动效和当前语言状态样式。

## 语言切换控件对齐优化

- 目标：参考视觉稿优化 CN/EN 切换控件的外框、分隔线、渐变滑块和高亮对齐。
- 主要文件：
  - `fluxa-web/src/features/articles/components/top-navigation.tsx:70`：固定滑块起始位置和左右位移距离，避免高亮位置偏移。

## 语言切换控件文字优化

- 目标：将语言切换控件文案改为小写并缩小字号，使视觉更轻。
- 主要文件：
  - `fluxa-web/src/features/articles/components/top-navigation.tsx:70`：调整切换控件字号和 `cn`、`en` 显示。

## 语言切换首屏闪烁修复

- 目标：避免切换页面或刷新时先显示英文，再由客户端状态切换为中文。
- 主要文件：
  - `fluxa-web/src/app/layout.tsx:1`：从语言 Cookie 读取服务端初始语言，并设置页面 `lang`。
  - `fluxa-web/src/app/providers.tsx:3`：向全局语言 Provider 传入初始语言和初始化等待状态。
  - `fluxa-web/src/features/i18n/i18n.tsx:6`：语言状态同步写入 Cookie 和 localStorage，没有 Cookie 时延迟显示直到客户端语言初始化完成。
